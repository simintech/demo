 b="block_";j=1; i = 1; while I <= getobjcount do begin //для всех объектов на схеме
 id = getobj(i); //получаем идентификатор объекта
 
 //если объект является интегратором, то устанавливаем ему коэффициент усиления
 if getpropasstring(id,"ClassName") = "Интегратор" then begin      
     setprop(id,"k",0.33);
  end;
 
 //если объект является цветным кругом, то задаем его цвет
 if getpropasstring(id,"ClassName") = "FillCircle" then begin      
     setpropevalstring(id,"color",255);
  end;
  
 if (getpropasstring(id,"ClassName") <> "Математическая связь") and (getpropasstring(id,"ClassName") <> "Label") and (getpropasstring(id,"ClassName") <> "FillCircle") then begin
     num=floattostr(j);  
     setpropevalstring(id,"Name", b+num); j=j+1;
  end;
 i = i + 1;
 end;     